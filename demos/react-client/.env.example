# MrWhoOidc React Demo - Environment Configuration

# =============================================================================
# OIDC Provider Configuration
# =============================================================================

# OIDC Authority (issuer URL)
# Default: https://localhost:8443 for local docker-compose deployment
VITE_OIDC_AUTHORITY=https://localhost:8443

# Client ID registered in MrWhoOidc Admin UI
# You must register this client before running the demo
VITE_OIDC_CLIENT_ID=react-demo

# Scopes requested during authentication
# openid: Required for OIDC
# profile: User profile information
# email: User email address
VITE_OIDC_SCOPE=openid profile email

# Redirect URI after successful authentication
# Must match the URI configured in Admin UI
# Default: http://localhost:5173/callback (Vite default port)
VITE_REDIRECT_URI=http://localhost:5173/callback

# Post-logout redirect URI
# Where user is redirected after logout
# Default: http://localhost:5173/
VITE_POST_LOGOUT_REDIRECT_URI=http://localhost:5173/

# =============================================================================
# Client Registration Instructions
# =============================================================================
#
# 1. Start MrWhoOidc: docker compose up -d
# 2. Access Admin UI: https://localhost:8443/admin
# 3. Login with default credentials: admin / Admin123!
# 4. Navigate to "Client Management" → "Create Client"
# 5. Configure:
#    - Client ID: react-demo
#    - Client Type: Public (SPA cannot securely store secrets)
#    - Grant Types: Authorization Code
#    - Redirect URIs: http://localhost:5173/callback
#    - Post Logout Redirect URIs: http://localhost:5173/
#    - Allowed Scopes: openid, profile, email
#    - PKCE Required: Yes (critical for public clients)
# 6. Save this file as .env in the demo directory
# 7. Install dependencies: npm install
# 8. Run: npm run dev
# 9. Access demo: http://localhost:5173
#
# =============================================================================
