# Copy this file to .env and set strong values before running docker compose
# Docker Compose automatically loads variables from a .env file in the project root

# ASP.NET Core dev certificate password (used by mrwho and admin containers)
ASPNETCORE_Kestrel__Certificates__Default__Password=your-cert-password

# Local path to HTTPS certs folder (mounted read-only into containers)
LOCAL_HTTPS_CERT_DIR=./certs

# Database root/admin passwords for different engines
MSSQL_SA_PASSWORD=YourStrong!Passw0rd
MYSQL_ROOT_PASSWORD=YourStrong!Passw0rd
MARIADB_ROOT_PASSWORD=YourStrong!Passw0rd
POSTGRES_PASSWORD=YourStrong!Passw0rd
# Copy to .env and fill values

# Strong SA password for local dev SQL Server
MSSQL_SA_PASSWORD=ChangeMe123!

# Password you use when exporting your dev HTTPS cert (see certs/README.md)
ASPNETCORE_Kestrel__Certificates__Default__Password=ChangeMe123!

# Directory on your host that contains aspnetapp.pfx (mounted read-only into containers as /https)
# On Windows (PowerShell), you can use something like: C:\\Users\\<you>\\.aspnet\\https
LOCAL_HTTPS_CERT_DIR=C:\\Users\\rum2c\\.aspnet\\https

# If you switch to MySQL in docker-compose (mysql service)
MYSQL_ROOT_PASSWORD=ChangeMe123!

# If you switch to PostgreSQL in docker-compose (postgres service)
POSTGRES_PASSWORD=ChangeMe123!
